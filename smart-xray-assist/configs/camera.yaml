camera:
  provider: mock                 # mock | realsense | orbbec | depthai
  model: D455
  serial: auto
  width: 640                     # MVP default per camera.md frame settings
  height: 480
  fps: 30
  align_depth_to_color: false

quality:
  min_valid_pixel_ratio: 0.85
  max_frame_drop_per_min: 30
  min_confidence: 0.80
